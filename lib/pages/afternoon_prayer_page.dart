import 'package:flutter/material.dart';

class AfternoonPrayerPage extends StatelessWidget {
  const AfternoonPrayerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('შუადღის ლოცვა'),
      ),
      body: const Center(
        child: Text('Content for the afternoon prayer will be added here.'),
      ),
    );
  }
}
