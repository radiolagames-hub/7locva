import 'package:flutter/material.dart';

class MidnightPrayerPage extends StatelessWidget {
  const MidnightPrayerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('ღამის ლოცვა'),
      ),
      body: const Center(
        child: Text('Content for the midnight prayer will be added here.'),
      ),
    );
  }
}
